# Logs

This directory is used by TURING to store runtime logs.

Log files are generated while the agent is running and may contain information about user interactions, tool calls, errors, and other runtime events.

Runtime logs are **not included in the repository**.

The `.gitignore` file keeps generated log files excluded while allowing this README to remain tracked.