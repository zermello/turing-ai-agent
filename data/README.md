# Data

This directory is used by TURING to store runtime data.

The application may create files such as the SQLite database used for persistent memory.

Runtime data is **not included in the repository** because it may contain user-specific information.

The `.gitignore` file keeps runtime files excluded while allowing this README to remain tracked.